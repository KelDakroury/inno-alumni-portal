import SimpleBar from 'simplebar-react'; // Importing SimpleBar component from 'simplebar-react'
import { styled } from '@mui/material/styles'; // Importing styled from '@mui/material/styles' for styling components

/**
 * Creates a styled component named Scrollbar using styled from '@mui/material/styles'.
 * The Scrollbar component is styled using the styled API from Material-UI.
 * It wraps the SimpleBar component to provide custom scrollbar styling.
 */
export const Scrollbar = styled(SimpleBar)``;
